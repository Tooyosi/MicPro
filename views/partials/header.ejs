
<!DOCTYPE html>
<html>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <head>
        <title>MIC PRO</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <link rel="stylesheet" href="/stylesheets/style.css">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    </head>
    <body>
    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only"> Toggle Navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>            
            <a class="navbar-brand" href="/">
                <img src="../../../../images/Oyo-State-emblem.png" class="img-thumbnail img-responsive emblem" alt="Oyo state emblem" width="50" height="66">
            </a>
            </div>
            <div id="navbar" class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                    <li class="<%= typeof page!=='undefined' && page == 'home' ? 'active' : '' %>"><a href="/">Home</a></li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
          
                        <li class="<%= typeof page !=='undefined' && page == 'staff' ? 'active' : ''%>"><a href="/staffs">Our Staffs</a></li>                    
                <%  if(!currentUser){ %>
                        <li class="<%= typeof page !=='undefined' && page == 'login' ? 'active' : ''%>"><a href="/login">Login</a></li>
                        <li class="<%= typeof page !=='undefined' && page == 'signup' ? 'active' : ''%>"><a href="/register">Sign Up</a></li>
                    <% }else{ %>
                        <li class="<%= typeof page !=='undefined' && page == 'profile' ? 'active' : ''%>"><a href="/user/<%= currentUser.id %>">My Profile</a></li>
                        <li><a href="/logout">Logout</a></li>
                  <% } %>
                </ul>
                <% if( typeof page !=="undefined" && page == "staff"){%>
                <form action="/staffs" method="GET" class="nav navbar-nav navbar-form navbar-right">
                    <div class="form-group">
                        <input type="search" class="form-control" name="search" id="" placeholder="search staffs">
                            <button class="btn btn-default" type="submit">
                              <i class="glyphicon glyphicon-search"></i>
                        </div>
                    </div>
                </form>
                <%}%>
            </div>
        </div>
    </nav>   
<div class="container-fluid contents">
   
<div>
    <% if(error && error.length > 0){%>
        <div class="alert alert-danger" role="alert">
            <%= error %>    
        </div>
    <% }%>
    <%if(success && success.length > 0){%>
        <div class="alert alert-success" role="alert">
            <%= success %>    
        </div>
    <% }%>
</div> 
